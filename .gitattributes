* text=auto eol=lf

# Windows batch files of the Maven wrapper: shipped with CRLF by Maven, and
# cmd.exe relies on it. Deliberate exception to the LF rule.
*.cmd text eol=crlf
*.bat text eol=crlf

*.jar binary
*.png binary
*.jpg binary
